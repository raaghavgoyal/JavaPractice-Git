package Test;

public class sql {
    //SQL questions
    //list all employess (emp name  => dept name)
    // of all departments for which employe age> 20 and department band > 10000

    //employee(empId, empName, deptId, empAge)
    //department(deptId, deptName, deptBand)

    //Select e.empName, d.deptName
    //from employee e
    //join department d
    //on e.deptId = d.deptId
    //where e.age>20 and d.deptBand>10000

    //Find customers who never placed an order
    //customers table: customer_id, customer_name
    //orders table: order_id, customer_id,
    //
}
